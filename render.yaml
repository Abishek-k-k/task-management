services:
  - type: worker
    name: taskflow-scheduler
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python scheduler.py

  - type: worker
    name: taskflow-scheduler
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python scheduler.py
